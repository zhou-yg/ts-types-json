/**
 * @jsxFactory h
 */
import { h } from 'my-lib';  
/**
 * 
 */
export const logic = () => {
  return {
  }
}

export const layout = (props) => {
  return (
    <div>

    </div>
  )
}

export const designPattern = (props) => {

}

export const style = (props) => {
  
}
